server:
  port: 8081
spring:
  application:
    name: my-app
  datasource:
    url: jdbc:mariadb://localhost:3306/hr
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
   